  <div class="result">
    <% if @judge.post.judge_correct_id == @judge.judge_correct_id %>
      <h3>正解</h3>
    <% else %>
      <h3>ぶぶー。間違い</h3>
    <% end %>
  </div>
  <%= link_to 'クイズトップへ', posts_path, class: "skill-edit-btn" %>
